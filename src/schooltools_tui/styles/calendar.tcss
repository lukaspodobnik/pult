EditClosuresScreen #edit-closures-screen {
    height: 1fr;
    padding: 1 2;
}

EditClosuresScreen #closures {
    height: 1fr;
    min-height: 8;
    border: round $primary;
    border-title-color: $text-accent;
}
