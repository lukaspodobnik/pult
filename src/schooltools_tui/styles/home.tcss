HomeScreen #main {
    height: 1fr;
}

HomeScreen #picker {
    width: 24;
    padding: 1;
    background: $panel;
    border-right: tall $primary;
}

HomeScreen .picker-entry {
    height: 3;
    padding: 1 2;
    color: $foreground-muted;
}

HomeScreen .picker-entry:hover {
    background: $primary-muted;
    color: $text-primary;
}

HomeScreen #content {
    width: 1fr;
    padding: 1 2;
}

HomeScreen #page-title {
    height: 3;
    padding-left: 1;
    content-align-vertical: middle;
    color: $text-accent;
    text-style: bold;
}

HomeScreen #schedule {
    height: 1fr;
    min-height: 10;
    padding: 1 2;
    background: $surface;
    border: round $primary;
}

HomeScreen #next-lesson,
HomeScreen #school-year {
    height: 5;
    margin-top: 1;
    padding: 1 2;
    background: $surface;
    border: round $secondary;
}
