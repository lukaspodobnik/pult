MainScreen #main {
    height: 1fr;
}

MainScreen #navigation {
    width: 24;
    padding: 1;
    background: $panel;
    border-right: tall $primary;
}

MainScreen #view-picker,
MainScreen #management-picker {
    height: 1fr;
}

MainScreen .picker-entry {
    height: 3;
    padding: 1 2;
    color: $foreground-muted;
}

MainScreen .picker-entry:hover {
    background: $primary-muted;
    color: $text-primary;
}

MainScreen #content {
    width: 1fr;
    padding: 1 2;
}

MainScreen #page-title {
    height: 3;
    padding-left: 1;
    content-align-vertical: middle;
    color: $text-accent;
    text-style: bold;
}

MainScreen #schedule {
    height: 1fr;
    min-height: 10;
    padding: 1 2;
    background: $surface;
    border: round $primary;
}

MainScreen #next-lesson,
MainScreen #school-year {
    height: 5;
    margin-top: 1;
    padding: 1 2;
    background: $surface;
    border: round $secondary;
}

EditTimetableScreen #edit-timetable-screen {
    height: 1fr;
    padding: 1 2;
}

EditTimetableScreen #edit-timetable-screen-title {
    height: 3;
    content-align-vertical: middle;
    text-style: bold;
}

EditTimetableScreen #edit-schedule {
    height: 1fr;
    min-height: 10;
    background: $surface;
    border: round $primary;
}

EditTimetableScreen #edit-timetable-screen-actions {
    height: auto;
    margin-top: 1;
    align-horizontal: right;
}

EditTimetableScreen #edit-timetable-screen-actions Button {
    margin-left: 1;
}
